server:
  port: 8000
  read_timeout: 2h
  write_timeout: 2h
  body_limit: 2048   # MB

rate_limit:
  requests_per_second: 100
  burst: 200
  expiration: 60

routes:
  - prefix: /buckets
    upstream: http://localhost:8080
